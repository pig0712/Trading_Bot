[build-system]
requires = ["setuptools>=68", "wheel", "numpy>=1.24"]
build-backend = "setuptools.build_meta"

[project]
name = "btcore"
version = "0.1.0"
description = "C accelerated backtest core (MA cross, long-only)"
requires-python = ">=3.12"
